services:
  broker-service:
    image: ghcr.io/mcp-bank/broker-service:latest
    ports:
      - "50051:50051"
  mcp-server:
    image: ghcr.io/mcp-bank/mcp-server:latest
    ports:
      - "8080:8080"
    depends_on:
      - broker-service
      - cache
  cache:
    image: redis:alpine